let game;

class Board{
  constructor(){
    this.currentBoard = ['-','-','-','-','-','-','-','-','-'];
  }
}

const player = (name) => {
  const getName = () => name;
  this.symbol = null;
  return {getName, symbol};
}

